---
# {{ ansible_managed }}
# Configuration to scrape prometheus instances

{% for host in groups['prometheus'] -%}
- targets:
    - "{{ hostvars[host]['ansible_default_ipv4']['address'] }}:{{ prometheus_port }}"
  labels:
    instance: {{ hostvars[host]['ansible_hostname'] }}
{% endfor %}
